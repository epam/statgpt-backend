Review the changes made during this session and clean them up:

1. First, run the `code-simplifier` agent to simplify and refine the recently modified code for clarity, consistency, and maintainability while preserving all functionality.

2. Then, run the `code-style-enforcer` agent to ensure the code follows project coding standards defined in CODE_STYLE.md - this includes removing unnecessary inline comments, consolidating imports at the top of files, and simplifying overly complex code.

Focus on files that were modified in this session. If $ARGUMENTS is provided, focus on those specific files instead.
